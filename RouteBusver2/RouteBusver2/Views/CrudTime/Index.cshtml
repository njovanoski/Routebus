@model IEnumerable<RouteBusver2.Time>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Time1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Stop.StopName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Trasport.BusName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Time1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Stop.StopName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Trasport.BusName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TimeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TimeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TimeID })
        </td>
    </tr>
}

</table>
